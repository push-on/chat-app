```bash
# tailwind
pnpm i -D tailwindcss postcss autoprefixer
pnpm i class-variance-authority clsx tailwind-merge
pnpm tailwindcss init -p
pnpm i @tailwindcss/forms
# redis, next auth, redis adapter
pnpm i @upstash/redis next-auth @next-auth/upstash-redis-adapter
pnpm i react-hot-toast lucide-react
```
